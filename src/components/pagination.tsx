import { Pagination } from "antd";
import React from "react";

const AppPagination = () => <Pagination className="app-pagination" defaultCurrent={1} current={1} total={50}/>

export default AppPagination